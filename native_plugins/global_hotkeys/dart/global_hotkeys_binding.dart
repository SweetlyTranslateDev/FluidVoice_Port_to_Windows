/// Native hotkey source binding stub.
class GlobalHotkeysBinding {
  const GlobalHotkeysBinding();

  bool get isAvailable => false;

  void start() =>
      throw UnimplementedError('global_hotkeys not linked (Phase 1)');

  void stop() {}
}
